{
  "mcpServers": {
    "example-data-source": {
      "command": "uvx",
      "args": ["some-mcp-server"],
      "env": {
        "NOTE": "put real keys in your shell env or .env, never in this file (H1); rename to .mcp.json to activate"
      }
    }
  }
}
