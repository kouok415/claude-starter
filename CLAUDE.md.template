# {{PROJECT_NAME}}

<!-- One paragraph: what this project is, who it's for, what success looks like.
     Replace with real content before first commit. -->

## Stack & layout

<!-- Fill this in after running your language's init command
     (npm init / uv init / cargo init / flutter create / etc.) -->

- **Language:** <e.g., TypeScript, Python 3.12, Rust>
- **Framework:** <e.g., Next.js 15, FastAPI, Axum>
- **Source layout:** <e.g., `src/` for code, `tests/` for tests>
- **Package manager:** <bun / pnpm / uv / cargo>

## Commands

- **Install:** `<command>`
- **Dev:** `<command>`
- **Test:** `<command>`
- **Lint:** `<command>`
- **Build:** `<command>`

## Project rules

<!-- Anything specific to this project that overrides or supplements global
     defaults. Keep this short. Only put deviations from global behavior here. -->

- _(none yet)_

## First-session protocol

1. Read `.ai_context/INDEX.md`, then `.ai_context/state.md`.
2. Follow INDEX's reading protocol for any other files relevant to the task.
3. Write progress to `state.md` as work progresses.

---

> This project uses `.ai_context/` as Claude's persistent project memory.
> See `.ai_context/INDEX.md` for the layout. Safe to skip if you're only
> here for the code.
